{
  "name": "@dsa/voice",
  "version": "0.1.0",
  "private": true,
  "description": "DSA voice: loudness-envelope analysis (OUI vs OUIOUIOUI), calibration, answer decision, speakable French phrasing, transcribe client types. Pure TypeScript.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "bundle-function": "node scripts/bundle-function.mjs",
    "smoke:groq": "node scripts/groq-smoke.mjs"
  },
  "dependencies": {
    "@dsa/core": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
